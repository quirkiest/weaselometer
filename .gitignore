# Environment / secrets
.env
.env.local
.env.production

# data/weaselwords.json is the SINGLE source of truth and IS tracked in git.
# Only ignore the transient bits:
data/weaselwords.json.*
data/.ww-*
data/*.lock
data/backups/

# Python artifacts
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Virtual environments
venv/
.venv/
env/

# IDE / OS
.DS_Store
.vscode/
